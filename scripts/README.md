# Build / Data Scripts

Codex should add build-time utilities here, especially `generate-holidays.ts` for producing static holiday JSON for the current year plus the next 2–3 years.

Runtime must consume generated static data and remain GitHub Pages compatible.
