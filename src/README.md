# Source Placeholder

Codex should initialize the React + TypeScript + Vite application here or replace this placeholder with the generated source tree.

Expected areas after implementation include components, pages, layouts, features, lib, types, styles and assets.
